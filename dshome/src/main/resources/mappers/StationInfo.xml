<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ds.home.realestate.mapper.StationInfoMapper">

	<select id="list" resultType="stationInfo">
		SELECT StationLineNo, 
			   StationLineName 
		  FROM StationInfo 
	  GROUP BY StationLineNo 
	  ORDER BY StationLineName
	</select>
	
	<select id="detail" parameterType="String" resultType="stationInfo">
		SELECT StationNo, 
			   StationName 
		  FROM StationInfo 
		 WHERE StationLineNo = #{stationLineNo}
	  ORDER BY StationName
	</select>

</mapper>