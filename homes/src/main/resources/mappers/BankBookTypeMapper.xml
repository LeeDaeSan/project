<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.homes.ds.mapper.BankBookTypeMapper">

	<!-- 통장 유형 목록 조회 -->
	<select id="select" resultType="bankBookType">
		SELECT
			BankBookTypeIdx,
			TypeName,
			TypeCode
			
		  FROM BankBookType
	</select>
	
</mapper>